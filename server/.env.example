PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# Optional: fill these in if you want the contact form to send a real email
# using nodemailer (see server/index.js for the commented-out setup).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO_EMAIL=hello@nathaytechnologies.com
